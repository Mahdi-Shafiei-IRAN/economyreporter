# TODO — وضعیت پیشرفت پروژه‌ی economy

> این فایل هر سری به‌روزرسانی می‌شود. ✅ = انجام و تست‌شده، 🔄 = در حال انجام، ⬜ = مانده.
> آخرین به‌روزرسانی: ۱۴۰۵/۰۷/۰۲ (2026-09-24)

## راه‌اندازی اولیه
- ✅ ساختار مستندات `docs/` (معماری، پارسر، نقشه‌ی راه، dev-setup)
- ✅ `CLAUDE.md` (قوانین دائمی پروژه)
- ✅ `.gitignore` (Django + Flutter)
- ✅ اسکلت پروژه‌ی Flutter در `mobile/`

## فاز ۱ — برش عمودی نازک (تک‌گوشی، آفلاین)
هدف: پیامک → تشخیص بانک از سرشماره → استخراج مبلغ → ذخیره در SQLite → لیست.

- ✅ **هسته‌ی پارسر (Dart خالص، تست‌شده — ۱۶ تست پاس، analyze تمیز)**
  - ✅ نرمال‌سازی ارقام فارسی/عربی → لاتین + حذف جداکننده‌ها (`digit_utils.dart`)
  - ✅ رجیستری بانک‌ها (`bank_registry.dart`) — بر پایه‌ی نام فرستنده. ⚠️ سرشماره‌های عددی (`senderCodes`) با نمونه‌ی واقعی پر شوند.
  - ✅ تشخیص بانک از روی فرستنده (قدم اول پارس)
  - ✅ استخراج مبلغ (ریال، با تبدیل تومان) + نوع تراکنش (income/expense/transfer)
  - ✅ استخراج مانده و ۴ رقم کارت (شامل کارت ماسک‌شده)
  - ⬜ استخراج پذیرنده/counterparty (فیلد آماده است؛ منطق استخراج با نمونه‌ی واقعی)
  - ⬜ استخراج تاریخ (نیازمند تبدیل تقویم شمسی — بعداً)
  - ✅ مدل `ParsedTransaction` (`models.dart`)
  - ✅ **تست واحد و اجرای واقعی (`flutter test`) — پاس شد**
- ✅ **لایه‌ی SQLite (تست‌شده — روی ویندوز با sqflite_common_ffi)**
  - ✅ اسکیما و باز کردن دیتابیس (`core/database/app_database.dart`) — جدول‌های transactions و outbox + ایندکس‌ها + ایندکس یکتای جزئی ضدتکرار
  - ✅ مدل ردیف `TransactionRecord` + نگاشت از/به Map و از `ParsedTransaction`
  - ✅ اثرانگشت پیامک `smsFingerprint` (`core/sms/sms_fingerprint.dart`)
  - ✅ مخزن `TransactionRepository`: درج، ضدتکرار، لیست، `summary` (درآمد/هزینه/مانده با حذف transfer)
  - ✅ کمک‌تابع تست FFI با fallback به winsqlite3 (`test/helpers/db_test_helper.dart`)
  - ✅ **۶ تست دیتابیس — همه پاس**
- ✅ **UI داشبورد (تست‌شده با widget test — headless، بدون گوشی)**
  - ✅ قالب‌بندی پول (نمایش به تومان) `core/format/money_format.dart`
  - ✅ `DashboardController` (بارگذاری جمع/لیست + افزودن از پیامک)
  - ✅ اینترفیس `TransactionStore` (تزریق‌پذیر: SQLite در اپ، fake در تست)
  - ✅ صفحه‌ی داشبورد: کارت جمع (درآمد/هزینه/مانده) + لیست + شیت «افزودن پیامک»
  - ✅ چیدمان راست‌به‌چپ + بازنویسی `main.dart`
  - ✅ **۱۰ تست UI/format (مجموع کل: ۳۱ تست، همه پاس)**
- ⬜ SMS Receiver اندروید (دریافت پیامک واقعی) — نیازمند گوشی/امولاتور + Android SDK
- ⬜ **اجرای بصری اپ روی گوشی/امولاتور** (تا این نباشد، دریافت واقعی پیامک تست نمی‌شود)

> وضعیت فاز ۱: هسته‌ی منطقی (پارسر + دیتابیس + UI) کامل و تست‌شده. تنها بخش وابسته به سخت‌افزار (دریافت پیامک و اجرای بصری) مانده که به Android SDK + گوشی/امولاتور نیاز دارد.

## فاز ۲ — پایه‌ی بک‌اند Django (روی کامپیوتر کاربر)
- ✅ **پروژه‌ی Django + DRF + JWT (تست‌شده — ۱۴ تست + smoke واقعی روی سرور)**
  - ✅ ساختار `backend/` با settings چندمحیطی (base/development/production)
  - ✅ دیتابیس تزریق‌پذیر: SQLite پیش‌فرض dev، Postgres با متغیر محیطی
  - ✅ کاربر سفارشی با UUID و ورود با ایمیل (`apps/users`)
  - ✅ endpointهای auth: register / login (JWT) / refresh / me
  - ✅ خانواده و عضویت (`apps/families`) + سقف ۳ نفر (server-side)
  - ✅ endpointهای family: ساخت/لیست، اعضا، دعوت (فقط مالک)، حذف عضو
  - ✅ Authorization سطح‌شیء (غیرعضو=۴۰۴ تا وجود خانواده لو نرود، عضو غیرمالک=۴۰۳)
  - ✅ requirements/{base,development,production}.txt + `.env.example`
  - ✅ **۱۴ تست (`manage.py test`) همه پاس + تست عملی روی سرور زنده (register/login/me/family)**
- ⬜ اجرای روی Postgres واقعی (وقتی سرور خریده شد یا Docker بالا آمد)

## فاز ۳ — مدل داده‌ی اصلی بک‌اند
- ✅ **مدل‌ها + مایگریشن + تست (۱۴ تست مدل، مجموع بک‌اند: ۲۸ پاس)**
  - ✅ `apps/accounts`: `BankAccount` (+ bank_id هم‌راستا با پارسر) و `Card` (unique در هر حساب)
  - ✅ `apps/categories`: `Category` (unique نام در هر خانواده)
  - ✅ `apps/transactions`: `Transaction` — نوع income/expense/**transfer**، `amount_rial`، `balance_after_rial`، `counterparty`، `transfer_group`، سه زمان (transaction_date/client_created_at/server_received_at)، ایندکس‌ها، و **ایندکس یکتای جزئی ضدتکرار** (family+source_message_hash)
  - ✅ `apps/budgets`: `Budget` (unique در category+period)
  - ✅ رفتار SET_NULL هنگام حذف دسته/حساب/کارت (تراکنش حفظ می‌شود)

## فاز ۴ — CRUD API + Sync + داشبورد
- ✅ **API کامل + تست (۱۶ تست API، مجموع بک‌اند: ۴۴ پاس)**
  - ✅ `apps/common`: تشخیص خانواده‌ی کاربر (`resolve_family`) + ابزار تست
  - ✅ ViewSet برای accounts/cards/categories/budgets/transactions با **ایزوله‌سازی خانواده** (queryset محدود + غیرعضو ۴۰۴)
  - ✅ فیلترهای تراکنش: kind/category/account/card/member/needs_review
  - ✅ **`POST /api/v1/sync/transactions/`** — آپلود دسته‌ای idempotent با پاسخ per-item (created/already_exists/error)؛ dedup دولایه (id + اثرانگشت)
  - ✅ **`GET /api/v1/dashboard/summary/`** — جمع درآمد/هزینه/مانده + تفکیک عضو/دسته/کارت (transfer حذف می‌شود)
  - ✅ تراکنش id از دستگاه می‌گیرد (idempotency)؛ ولیدیشن ارجاع‌ها (حساب/کارت/دسته هم‌خانواده)

## فاز ۵ — اتصال موبایل به بک‌اند
- ✅ **لایه‌ی شبکه/احراز هویت + تست (۶ تست، مجموع موبایل: ۳۷ پاس)**
  - ✅ `core/config/app_config.dart`: آدرس API (قابل override با --dart-define)
  - ✅ `core/auth/token_store.dart`: ذخیره‌ی امن توکن (اینترفیس + SecureTokenStore با Keystore)
  - ✅ `core/network/api_client.dart`: Dio + interceptor الصاق توکن و **تازه‌سازی خودکار روی 401**
  - ✅ `core/auth/auth_repository.dart`: register/login/me/logout + UserProfile
  - ✅ `features/auth`: `AuthController` + `LoginScreen`
  - ✅ `main.dart`: مسیریابی ورود↔داشبورد + دکمه‌ی خروج
  - ✅ تست: auth_repository (login/me/register/refresh با mock Dio) + widget ورود (موفق/ناموفق)
- ⬜ اتصال بصری واقعی به سرور (نیازمند اجرای اپ روی گوشی/امولاتور + Developer Mode ویندوز برای بیلد پلاگین)

## فاز ۸ — Background Sync (outbox → سرور)
- ✅ **موتور sync + تست (۸ تست، مجموع موبایل: ۴۵ پاس)**
  - ✅ صف‌کردن خودکار تراکنش معتبر در `outbox` هنگام ذخیره (نوع نامشخص صف نمی‌شود)
  - ✅ `core/sync/remote_transaction_api.dart`: کلاینت `POST /sync/transactions/` (اینترفیس + Dio)
  - ✅ `core/sync/sync_service.dart`: خواندن pending، آپلود دسته‌ای (۵۰تایی)، اعمال نتیجه‌ی per-item
  - ✅ idempotency: sync دوباره چیزی نمی‌فرستد؛ created/already_exists → synced
  - ✅ retry با backoff (۳۰ثانیه→۲دق→۱۰دق→۳۰دق)؛ **خطای شبکه آیتم‌ها را گم نمی‌کند**
  - ✅ سیم‌کشی در `main.dart` (sync اولیه) + دکمه‌ی همگام‌سازی در داشبورد
- ⬜ تشخیص خودکار اتصال (connectivity) برای trigger — فعلاً sync اولیه + دکمه‌ی دستی
- ⬜ تست شبکه‌ی واقعی به سرور روی کامپیوتر (نیازمند گوشی/امولاتور)

## فاز ۶ — موتور تراکنش محلی + صف بازبینی
- ✅ **ویرایش/حذف/فیلتر + صف بازبینی + تست (۹ تست، مجموع موبایل: ۵۴ پاس)**
  - ✅ مخزن: `getAll` با فیلتر (kind/needsReview/search)، `needsReviewCount`، `updateTransaction`، `deleteTransaction`
  - ✅ ویرایشِ نوعِ نامشخص به معتبر → خودکار به outbox صف می‌شود؛ حذف، outbox را هم پاک می‌کند
  - ✅ `ReviewScreen`: انتخاب نوع (درآمد/هزینه/انتقال) + تأیید/حذف هر مورد
  - ✅ `EditTransactionSheet`: ویرایش نوع/مبلغ(تومان)/طرف‌حساب/توضیح + حذف (با ضربه روی ردیف)
  - ✅ داشبورد: نشان تعداد بازبینی در نوار بالا + ورود به صف بازبینی
  - ✅ `DashboardController`: needsReviewCount + updateTransaction/deleteTransaction/confirmReview

## فاز ۹ — تطبیق مانده + داشبورد خانواده از سرور
- ✅ **تطبیق مانده + داشبورد سرور + تست (۱۰ تست، مجموع موبایل: ۶۴ پاس)**
  - ✅ `core/reconcile`: `ReconciliationService` — از روی «مانده»ی پیامک، پیامک جاافتاده را کشف می‌کند (به‌تفکیک کارت، جهت برداشت/واریز)
  - ✅ داشبورد: بنر «N مورد احتمال پیامک جاافتاده» + `ReconciliationScreen`
  - ✅ `core/dashboard`: مدل `DashboardSummary` + `RemoteDashboardApi` (اینترفیس + Dio)
  - ✅ `FamilyDashboardScreen`: جمع خانواده از سرور + تفکیک عضو/دسته/کارت (با loading/error)
  - ✅ سیم‌کشی در `main.dart` + دکمه‌ی داشبورد خانواده در نوار بالا

## فاز ۱۱ — سخت‌سازی امنیت
- ✅ **rate limiting + حریم خصوصی + سند (تست: بک‌اند ۴۵، موبایل ۶۵)**
  - ✅ بک‌اند: Rate limiting روی ورود/ثبت‌نام (scope `auth`، ۱۰/دقیقه) ضد brute-force + تست
  - ✅ موبایل: تضمین **عدم ارسال متن خام پیامک** به سرور (تست whitelist کلیدهای payload)
  - ✅ موبایل: توکن در ذخیره‌ی امن پیش‌فرض (Keystore)
  - ✅ `docs/security.md`: جمع‌بندی وضعیت + کارهای مانده (SQLCipher/HTTPS/pinning در فاز تولید)
  - یادآوری: Authorization سطح‌شیء (فاز ۴) و عدم ذخیره‌ی PAN کامل (فاز ۳) از قبل انجام شده‌اند

## فاز ۱۰ — Conflict & Recovery
- ✅ **تست سناریوهای بازیابی (۴ تست، مجموع موبایل: ۶۹ پاس) — همه در اولین اجرا پاس (کد از قبل درست بود)**
  - ✅ پاسخِ گم‌شده / کرش وسط sync → ارسال دوباره تکراری نمی‌سازد (already_exists)
  - ✅ «۱۰ نه ۲۰»: صف‌شدن دوباره پس از کرش، رکورد تکراری روی سرور نمی‌سازد
  - ✅ batch نیمه‌موفق: موفق‌ها synced، خطادار در صف برای retry می‌ماند
  - ✅ قطع شبکه سپس بازگشت: آیتم‌ها گم نمی‌شوند و بعد از برگشت اینترنت (عبور از backoff) sync می‌شوند
  - یادآوری: انقضای JWT با interceptor لایه‌ی شبکه شفاف مدیریت می‌شود (فاز ۵)؛ migration دیتابیس هنگام تغییر schema اضافه می‌شود

## فاز ۷ — پارسر کامل + تست رگرسیون (بخش نرم‌افزاری)
- ✅ **افزودنی‌های پارسر + هارنس رگرسیون (۱۶ تست، مجموع موبایل: ۸۵ پاس)**
  - ✅ استخراج **تاریخ شمسی** و تبدیل به UTC (`core/sms/jalali.dart`) — تاریخ تراکنش پر می‌شود
  - ✅ استخراج **طرف حساب/پذیرنده** («بابت»/«به کارت»/«پذیرنده»)
  - ✅ **هارنس رگرسیون**: `test/fixtures/sms/*.json` + `test/fixtures_test.dart` (۶ نمونه‌ی مصنوعی)
  - ✅ کالیبره با ۲ نمونه‌ی واقعی: مبلغِ چسبیده به فعل بدون واحد، تشخیص بانک از متن، تاریخ دورقمی
  - ✅ **کلید حساب (`account_ref`)** + migration نسخه۲ → **تطبیق مانده برای بانک‌های حساب‌محور** (مثل تجارت) کار می‌کند
  - ⏳ **مانده تا نمونه‌ی بیشتر:** سرشماره‌های عددی بانک‌ها (کاربر بفرستد)، واحدِ پیش‌فرض/تومانیِ «مانده»، دسته‌بندی خودکار
  - 📌 وقتی نمونه رسید: فقط فایل json (با حساب/کارت ماسک‌شده) در پوشه‌ی fixtures اضافه می‌شود

## بیلد و اجرا روی گوشی
- ✅ **لوگو** (کارت بانکی) با flutter_launcher_icons (`assets/icon/`) + نام «مالی خانواده»
- ✅ **APK ساخته شد** — release جدا به‌ازای ABI (arm64 ~۱۶مگ) در `mobile/build/app/outputs/flutter-apk/`
- ✅ ارتقای toolchain اندروید: AGP 8.1 / Gradle 8.4 / Kotlin 1.9 / compileSdk 34 / minSdk 23 / JVM17
- ✅ پروکسی Gradle (`~/.gradle/gradle.properties`) + network security config برای HTTP سرور توسعه
- ✅ `flutter_secure_storage` به ^9.2.2 پین شد (سازگاری با toolchain)
- ~~کاربر تست ساخته شد: `test@economy.local` / `Test@12345` (+ خانواده)~~ — منسوخ: ورود حالا با شماره موبایل است؛ این کاربر را در پنل مدیریت حذف کن (`docs/run-on-phone.md` بخش ۳)
- ✅ راهنمای کامل: `docs/run-on-phone.md`
- ✅ **SMS Receiver ساخته شد** (نیازمند تست روی گوشی)

## SMS Receiver — خواندن خودکار پیامک
- ✅ **فیلتر رمز پویا (OTP)** — پیامک رمز پویا/یکبارمصرف تراکنش حساب نمی‌شود (تست‌شده)
- ✅ `SmsImporter` (خالص، تست‌شده): فقط تراکنش واقعی وارد می‌شود، تکراری رد می‌شود
- ✅ `SmsInboxService`: مجوز SMS، وارد کردن صندوق هنگام باز شدن اپ، listener زنده (پیش/پس‌زمینه)
- ✅ ذخیره‌ی خودکار + تازه‌سازی داشبورد + **sync خودکار** (داشبورد خانواده هم به‌روز شود)
- ✅ مجوزهای RECEIVE_SMS/READ_SMS + درخواست زمان اجرا
- ⏳ **تست روی گوشی خودت** (APK جدید ساخته شد)

## دسته‌بندی چندتایی + گزارش
- ✅ **مدل دسته‌ها + تخصیص چندتایی با تقسیم مساوی** (schema نسخه۳ + migration + seed ۱۵ دسته)
- ✅ `categorize` (تقسیم مساوی با باقی‌مانده)، `categoryTotals`، `uncategorized` (تست‌شده)
- ✅ صفحه‌ی دسته‌بندی: تیک‌زدن چند دسته + توضیح کوتاه (`CategorizeScreen`)
- ✅ بنر «نیازمند دسته‌بندی» روی داشبورد + فهرست دسته‌بندی‌نشده‌ها
- ✅ **گزارش دسته‌ها** با بازه‌ی امروز/هفته/ماه/کل + نوار نسبت (`CategoryReportScreen`)
- ✅ ۶ تست جدید (مخزن + ویجت)، مجموع موبایل ۱۰۲ تست

## نوتیفیکیشن دسته‌بندی
- ✅ **NotificationService** (flutter_local_notifications): نوتیفیکیشن «تراکنش جدید — دسته‌بندی کن»
- ✅ با لمس نوتیفیکیشن، `CategorizeScreen` همان تراکنش باز می‌شود (navigatorKey + getById)
- ✅ کار در پیش‌زمینه و پس‌زمینه (نوتیفیکیشن از ایزوله‌ی پس‌زمینه)
- ✅ مجوز POST_NOTIFICATIONS + desugaring در build.gradle
- ⏳ تست روی گوشی خودت (APK جدید ساخته شد)

## گزارش نموداری + PDF + دارک‌مود
- ✅ **دارک/لایت/سیستم مود** (`ThemeController` + shared_preferences، دکمه در نوار بالا) — تست‌شده
- ✅ **نمودار دایره‌ای** (fl_chart) در گزارش دسته‌ها + نوار رنگیِ هر دسته
- ✅ **خروجی PDF** گزارش (pdf + printing + فونت فارسی Vazirmatn) با جدول دسته/مبلغ/درصد
- ⏳ تست روی گوشی خودت (APK جدید ساخته شد)

## مدیریت حساب/کارت اعضا (کیف‌ها)
- ✅ **مدل Wallet** + جدول wallets (schema نسخه۴ + migration)
- ✅ مخزن: افزودن/فهرست/حذف کیف + تطبیق با کارت/حساب تراکنش (تست‌شده)
- ✅ `WalletsScreen`: ثبت کارت/حساب با **نام صاحب** + برچسب + بانک + ۴رقم کارت/شماره حساب
- ✅ روی هر تراکنش «صاحب • برچسب» نشان داده می‌شود (کدام شخص و کدام کارت) — تطبیق خودکار
- ✅ دکمه‌ی «حساب‌ها و کارت‌ها» در نوار بالای داشبورد
- ✅ ۵ تست جدید (مخزن + ویجت)، مجموع موبایل ۱۰۹ تست

## بازطراحی UI/UX + همگام‌سازی دوطرفه + مالکیت کارت (۱۴۰۵/۰۶/۲۰)
- ✅ **رفع «۱۰۷ ناموفق» در همگام‌سازی:** سرور فیلد متنیِ null (طرف حساب/مبلغ خام) را رد می‌کرد؛ حالا گوشی null نمی‌فرستد و سرور هم می‌پذیرد
- ✅ **همگام‌سازی دوطرفه:** دریافت تغییرات خانواده (`GET /sync/transactions/?since=`) + ارسال ویرایش‌ها (آخرین ویرایش برنده)، payload همیشه از آخرین نسخه، شناسه‌ی ثابت گوشی، جلوگیری از اجرای هم‌زمان، پیام قابل‌فهم (سرور در دسترس نبود / N ارسال، M دریافت)
- ✅ **مالکیت کارت:** کارت به عضو خانواده وصل می‌شود؛ فقط صاحب کارت ویرایش/دسته‌بندی/نامعتبر می‌کند، بقیه فقط می‌بینند (در اپ و در سرور)
- ✅ **ذخیره‌ی فوری پیامک:** گیرنده‌ی زنده‌ی SMS در Manifest ثبت شد (قبلاً نبود و پیامک فقط هنگام باز شدن اپ از صندوق خوانده می‌شد) + متن و زمان پیامک در دیتابیس گوشی (sync نمی‌شود)؛ ضدتکرار محتوایی بین دریافت زنده و صندوق
- ✅ **«نامعتبر است» = حذف نرم:** برای تراکنش انجام‌نشده/پیامک رمز؛ با خواندن دوباره‌ی صندوق برنمی‌گردد و روی گوشی بقیه هم حذف می‌شود
- ✅ **بازبینی فقط برای ابهام واقعی:** مبلغ/نوع نامشخص یا «ناموفق» (بانک ناشناخته دیگر دلیل نیست)؛ دلیل + متن پیامک + تاریخ؛ منتظرِ بازبینی در جمع نمی‌آید
- ✅ **پارسر:** رمز پویا با مبلغ («رمز: ۱۲۳۴۵۶»)، یادآوری/سررسید، تراکنش ناموفق؛ «به کارت» در واریز طرف حساب نیست؛ تاریخ جزو نام پذیرنده نمی‌شود
- ✅ **شروع دسته‌بندی از ماه بعد** (پیش‌فرض ۱ مهر ۱۴۰۵، قابل تغییر در تنظیمات)؛ تراکنش‌های قدیمی نه در صف دسته‌بندی نه نوتیفیکیشن
- ✅ **صفحه‌ی اصلی جدید:** ماه شمسی با «از … تا …»، نمای پله‌ای شخص ← کارت ← تراکنش، نمای «همه با هم» با سرتیتر روز، ترتیب (تاریخ/مبلغ)، فیلتر نوع/شخص، جستجو، انتخاب چندتایی (دسته‌بندی/نامعتبر گروهی)، برگه‌ی جزئیات، «تعیین صاحب» برای کارت بی‌صاحب
- ✅ **گزارش و نمودار:** روند روزانه/ماهانه (هزینه/درآمد جدا)، تفکیک افراد/کارت‌ها/دسته‌ها، PDF کامل (فارسی راست‌به‌چپ، فهرست تراکنش) با اشتراک یا چاپ
- ✅ **تطبیق مانده (پیامک جاافتاده):** توضیح + «ثبت دستی همین مبلغ» / «نادیده بگیر»؛ بدون هشدار اشتباه برای داده‌ی بی‌زمان
- ✅ **تنظیمات:** کارت‌ها، بازبینی، شروع دسته‌بندی، وضعیت و توضیح همگام‌سازی، تم؛ تم جدید با فونت Vazirmatn (روشن/تیره)
- ✅ **تست:** موبایل ۱۸۷ + بک‌اند ۵۷ پاس؛ تست زنده‌ی دو گوشی با سرور واقعی Django (`mobile/test_live/`)؛ اسکرین‌شات صفحه‌ها با فونت واقعی (`mobile/screenshot_test/`)؛ بیلد APK release
- ⏳ تست روی گوشی خودت با APK جدید (مخصوصاً دریافت پیامک وقتی اپ بسته است)

## پنل مدیریت + ورود با شماره + فقط پیامک فرستنده‌های مجاز (۱۴۰۵/۰۶/۲۰)
- ✅ **پنل مدیریت فارسی** (`/admin/`، راست‌به‌چپ، وقت تهران): ساخت کاربر با شماره موبایل + نام + رمز و انتخاب خانواده در همان صفحه؛ خانواده با اعضا (سقف ۳ نفر در پنل هم رعایت می‌شود)؛ تغییر رمز، غیرفعال/حذف کاربر؛ تراکنش‌ها (فیلتر خانواده/شخص/نوع، عمل «نامعتبر کن» که به گوشی‌ها هم می‌رسد)؛ حساب/کارت، دسته، بودجه
- ✅ **ورود با شماره موبایل + رمز** به‌جای ایمیل؛ هر شکل شماره (`+98`، ارقام فارسی، بدون صفر) استاندارد می‌شود؛ مایگریشن `users.0002` (کاربر ایمیلیِ قدیمی می‌ماند ولی تا شماره نگیرد وارد نمی‌شود)
- ✅ **ثبت‌نام عمومی API حذف شد** (امنیت روی سرور): کاربر فقط در پنل ساخته می‌شود؛ دعوت عضو با شماره
- ✅ **رفع باگ همگام‌سازی:** cursor دریافت فقط زمان بود و ردیف‌های با زمان برابر در مرز صفحه جا می‌افتادند (مثلاً بعد از عمل گروهی پنل) → cursor «زمان|شناسه» (cursor قدیمی هم پذیرفته می‌شود)
- ✅ **فقط پیامک فرستنده‌های مجاز:** اول فرستنده چک می‌شود؛ پیامک تبلیغ/فروشگاه حتی با مبلغ ثبت نمی‌شود. صفحه‌ی «فرستنده‌های پیامک بانک»: پیشنهاد از پیامک‌های مبلغ‌دارِ گوشی، «بانک است، مجاز کن» / «بانک نیست؛ حذف N تراکنش»، افزودن دستی با حدس بانک؛ تطبیق `+98`/`0`/ارقام فارسی؛ بانکِ فرستنده = قدم اول پارس (schema نسخه ۶)
- ✅ **انتخاب شخص بالای صفحه:** تراشه‌های «همه / بابا / مامان / …»؛ خالص و درآمد/هزینه‌ی بالا و فهرست فقط مال همان شخص (در هر دو نما)
- ✅ **متن پیامک روی هر تراکنش** (در تنظیمات قابل خاموش کردن) + متن کامل بالای برگه‌ی جزئیات و دکمه‌ی «حذف (اشتباه/نامعتبر)»
- ✅ کارت تازه پیش‌فرض مال خود کاربر؛ تراشه‌های «نیاز به توجه» چندخطی (همه دیده می‌شوند)؛ جمع روزِ بزرگ در صفحه‌ی باریک از کادر بیرون نمی‌زند
- ✅ رفع باگ مهاجرت: در مهاجرت نسخه ۵ یک return زودهنگام، مهاجرت‌های بعدی را روی دیتابیس‌های خیلی قدیمی جا می‌انداخت
- ✅ **تست:** بک‌اند ۷۱ پاس (ورود با شماره، پنل مدیریت، cursor)، موبایل ۲۱۲ پاس + analyze تمیز، تست زنده‌ی دو گوشی با سرور واقعی ۲/۲، ۱۴ اسکرین‌شات
- ✅ دیتابیس توسعه migrate شد (پشتیبان قبل از آن: `backend/db.backup-20260911.sqlite3`)
- ✅ **APK release جدید** ساخته شد: `mobile/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` (۱۹٫۱ مگ)
- ⬜ **کار خودت:** `createsuperuser` → در پنل خانواده و کاربرها را بساز → «کاربر تست» را حذف کن (`docs/run-on-phone.md` بخش ۳)
- ⏳ تست روی گوشی خودت با APK جدید (تعیین فرستنده‌های بانک، انتخاب شخص، متن پیامک)

## رفع دو باگ بعد از تست روی گوشی (۱۴۰۵/۰۶/۲۰)
- ✅ **«تعیین صاحب» و افزودن کارت کار نمی‌کرد:** پیامک بانک‌ها شماره‌ی کارت ندارد (روی سرور ۲۴۳ از ۲۴۴ تراکنش بی‌شماره؛ نمونه‌ی واقعی «حساب…» دارد نه کارت)، ولی کارت فقط با ۴ رقم کارت یا شماره حساب پیدا می‌شد و فرم بدون شماره ذخیره نمی‌شد. حالا کارتِ «فقط بانک» ثبت می‌شود و تطبیق به‌ترتیب: شماره‌ی کارت/حساب → کارتِ فقط‌بانکِ همان بانک → تنها کارتِ بی‌تضادِ آن بانک (ابهام → بی‌صاحب). برای پیامکِ بی‌بانک به‌جای «تعیین صاحب» راهنمای «فرستنده‌های پیامک بانک»؛ مجاز کردن فرستنده با بانک، بانکِ پیامک‌های قبلیِ همان فرستنده را هم پر می‌کند
- ✅ **«کاربر تست» هنوز دیده می‌شد:** گوشی با نشستِ قدیمیِ ایمیلی وارد مانده بود (سرور: آخرین همگام‌سازی به نام کاربر تست، خانواده‌ی جدید بی‌تراکنش). حالا حسابِ بی‌شماره و نشستِ ردشده (کاربر حذف/غیرفعال در پنل) خودکار به صفحه‌ی ورود می‌برد، با پیام؛ در قطعی شبکه نه
- ✅ **جابه‌جایی حساب روی یک گوشی:** داده‌ی خانواده‌ی قبلی از گوشی پاک، کارت‌های «من» به کاربر جدید، و تراکنش‌های پیامکِ همین گوشی با شناسه‌ی تازه به خانواده‌ی جدید فرستاده می‌شوند (قبلاً «تعارض شناسه» می‌گرفت و هیچ‌وقت نمی‌رسید)
- ✅ **تست:** موبایل ۲۲۸ پاس + analyze تمیز؛ تست زنده با سرور واقعی ۳/۳ (شامل ورود با حساب خانواده‌ی دیگر روی همان گوشی)
- ⏳ نصب APK جدید و تست روی گوشی: اپ باید خودش به صفحه‌ی ورود ببرد؛ با شماره‌ی خودت وارد شو، بعد «تعیین صاحب» روی کارت‌های بی‌صاحب (فقط بانک)

## استقرار روی سرور (اوبونتو ۲۴) (۱۴۰۵/۰۶/۲۳)
- ✅ **نصب‌کننده‌ی خودکار `deploy/install.sh`** (بدون Docker): همه‌چیز نام‌دار و مستقل تا با پروژه‌های دیگرِ سرور قاطی نشود — کاربر سیستم `economy`، پوشه‌ی `/opt/economy`، دیتابیس/نقش `economy`، سرویس `economy.service` (gunicorn روی سوکت یونیکس)، بلوک nginx، فایروال ufw، و گواهی HTTPS با certbot
- ✅ **ابزار مدیریت `economyctl`** (status/logs/update/migrate/superuser/passwd/backup/env/renew-cert) نصب در `/usr/local/bin`
- ✅ `.env` خودکار با `SECRET_KEY` تازه و رمز دیتابیسِ تصادفی؛ مقادیر موجود در اجرای دوباره حفظ می‌شوند
- ✅ تنظیمات امنیتی تولید env-محور شد (`SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`، پیش‌فرض True) + `CSRF_TRUSTED_ORIGINS` خودکار از ALLOWED_HOSTS؛ حالت `--no-ssl` برای تستِ HTTP روی IP
- ✅ `.gitattributes` برای LF ماندن اسکریپت‌های شل (وگرنه روی لینوکس خراب می‌شوند)
- ✅ راهنمای مرحله‌به‌مرحله‌ی فارسی: `deploy/README.md` (شامل گذاشتن چند پروژه‌ی جدا روی یک سرور)
- ⏳ **کار خودت روی سرور:** `sudo bash deploy/install.sh --domain <زیر‌دامنه> --email <ایمیل>` سپس `sudo economyctl superuser`؛ بعد APK را با `--dart-define=API_BASE_URL=https://<دامنه>/api/v1` دوباره بساز

## تکراری‌ها + امضای انتشار + کافه‌بازار (۱۴۰۵/۰۶/۲۴)
- ✅ **رفع باگ «حقوق یک بار، دوبار حساب شد»:** ضدتکرارِ محتوایی از ۱۰ دقیقه به ۳ روز گسترش یافت — متنِ کاملِ پیامک بانک (شامل مانده/زمان) اثرانگشتِ یکتاست، پس پیامکِ همسان = همان رخداد؛ متنِ متفاوت (مانده‌ی دیگر) تراکنشِ جدا می‌ماند
- ✅ **صفحه‌ی «تراکنش‌های تکراری»:** برای رخدادی که با دو پیامکِ *متفاوت* دوبار ثبت شده (`DuplicateFinder`: نوع+مبلغ+بانک+کارت یکسان در بازه‌ی ۶ ساعت)؛ تراشه‌ی «احتمال تکراری» در نوار توجه؛ «حذف تکراری‌ها» (قدیمی می‌ماند) / «تکراری نیست» (نادیده)
- ✅ **امضای انتشارِ اختصاصی:** کلید `economy-release.jks` ساخته شد، `signingConfig` از `key.properties` (هر دو در گیت نیستند)؛ APK حالا با `CN=Mahdi Shafiei` امضا می‌شود نه debug → هشدار «برنامه‌ی ناشناس/خطرناک» حذف
- ✅ **APK امضاشده‌ی جدید:** `mobile/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` (۱۹٫۸ مگ)، علیه `http://87.248.150.95` (IP در network security config)
- ✅ **راهنمای کافه‌بازار:** `docs/cafebazaar-publish.md` (ثبت‌نام، دسترسی پیامک، رابطه با OTA)
- ✅ **تست:** موبایل ۲۴۲ پاس (۶ تستِ تازه‌ی `DuplicateFinder` + تستِ ضدتکرارِ ۳ روزه) + analyze تمیز
- ⚠️ **کلید انتشار را بکاپ بگیر** (`mobile/android/app/economy-release.jks` + رمز `economy1404`). گمش کنی، آپدیتِ بعدیِ همین برنامه در بازار ممکن نیست.
- ⬜ **مانده:** ثبت‌نام خانواده و افزودن عضو توسطِ خودِ کاربر (حداکثر ۳ نفر) — نیازمند طراحی

## ثبت‌نام آزاد خانواده و افزودن عضو توسط کاربر (۱۴۰۵/۰۶/۲۴)
- ✅ **ثبت‌نامِ آزاد (بک‌اند):** `POST /auth/register/` (بدون احراز): شماره+رمز+نام → کاربر + خانواده‌ی تازه + عضویتِ مالک + توکن؛ با throttle. شماره‌ی تکراری رد می‌شود
- ✅ **افزودن عضو توسط مدیرِ خانواده (بک‌اند):** `POST /family/<id>/members/invite/` حالا اگر شماره نبود با رمزِ داده‌شده حسابِ تازه می‌سازد؛ سقفِ ۳ نفر روی سرور (`FAMILY_MAX_MEMBERS`) و در clean مدل
- ✅ **اپ:** صفحه‌ی ثبت‌نام (لینک زیر ورود)، صفحه‌ی «افزودن عضو خانواده» در تنظیمات (فقط مدیر، تا رسیدن به ۳ نفر)؛ `DashboardController.addMember`/`canAddMember`
- ✅ **تست:** بک‌اند ۷۶ پاس (ثبت‌نام، ساخت عضو با رمز)، موبایل ۲۴۶ پاس (۴ تستِ افزودن عضو) + analyze تمیز
- ⬜ **کار خودت:** نصب APKِ `1.0.2+3` و تستِ ثبت‌نام و افزودن عضو روی گوشی

## رفع ایرادهای تست روی گوشی (۱۴۰۵/۰۶/۲۴ - دور دوم)
- ✅ **حذف سقفِ ۳ نفرِ خانواده:** هم بک‌اند (invite/model/admin) هم اپ؛ مدیر هر تعداد عضو می‌تواند اضافه کند
- ✅ **حریمِ مدیر روی گوشیِ عضو:** بک‌اند از قبل درست بود (SyncView برای عضو فقط مالِ خودش را می‌دهد) ولی سرور به‌روزنشده بود؛ حالا اپ هم موقع دریافت، تراکنشِ غیرِخودِ عضو را کنار می‌گذارد (`_pull` با نقش) → عضو دیگر تراکنش/کارتِ مدیر را نمی‌بیند و نمی‌تواند دسته‌بندی/نام‌گذاری کند
- ✅ **گزینه‌ی «بانک نیست» برای همه‌ی پیشنهادها:** قبلاً فقط وقتی تراکنش ثبت‌شده داشت دیده می‌شد؛ حالا همیشه هست — فرستنده را نادیده می‌گیرد (dismissed) تا دیگر پیشنهاد نشود، و اگر تراکنش اشتباهی داشته حذفشان می‌کند
- ✅ **پاک‌سازی متن‌های زشت (placeholder):** زیرنویس‌های طولانیِ «هنوز در کارت‌ها…/بانکش معلوم نیست…» کوتاه شد؛ فقط برای کارتِ بی‌بانک یک راهنمای کوتاه می‌ماند
- ✅ **تست:** موبایل ۲۵۰ پاس (۳ تستِ فرستنده‌ی dismissed + تستِ حریمِ عضو + افزودن عضو بی‌سقف)، بک‌اند ۷۵ پاس
- ✅ **APK امضاشده‌ی `1.0.3+4`** ساخته شد (همان مسیر)
- ⬜ **مانده (فاز بعد):** همگام‌سازیِ حساب‌ها/کارت‌ها و دسته‌ها و بودجه‌ها با سرور (الان فقط تراکنش‌ها روی سرورند؛ endpointهای accounts/categories/budgets هستند ولی اپ هنوز push/pull نمی‌کند) — کارِ بزرگِ جداست
- ⚠️ **کارِ خودت روی سرور (برای اینکه حریمِ نقش‌ها واقعاً کار کند):** `sudo economyctl update && sudo economyctl migrate`، و در پنل نقشِ اعضای غیرمدیر را «عضو» کن

## هم‌گام‌سازی کیف‌ها (کارت/حساب) با سرور (۱۴۰۵/۰۶/۲۴ - دور سوم)
- ✅ **مدل سرور `Wallet`** (در اپ accounts): شناسه از گوشی می‌آید (UUID)، خانواده/صاحب/سازنده، برچسب/بانک/۴رقم/شماره‌حساب، حذف نرم، cursor به‌روزرسانی. شماره‌ی کاملِ کارت هرگز ذخیره نمی‌شود
- ✅ **endpoint `/wallets/sync/`**: POST آپلود دسته‌ای idempotent (upsert با id)، GET دریافت با cursor؛ **حریمِ نقش**: عضو فقط کیف‌های خودش را می‌گیرد، مدیر/ادمین همه‌ی خانواده
- ✅ **موبایل:** schema نسخه ۸ (ستون‌های `is_deleted/updated_at/client_updated_at/sync_status` روی wallets)؛ افزودن/ویرایش/حذفِ کیف حالا pending می‌شود و در sync آپلود؛ حذف = حذف نرم تا به گوشی‌های دیگر برسد؛ کیف‌های سرور در محلی درج و صاحبِ تراکنش‌ها دوباره تعیین می‌شود
- ✅ **در چرخه‌ی sync موجود**: بعد از تراکنش‌ها، کیف‌ها هم push/pull می‌شوند (خطایش کل sync را نمی‌شکند)
- ✅ **تست:** بک‌اند ۸۰ پاس (۵ تستِ WalletSync: upsert idempotent، حریمِ عضو، دیدِ مدیر، حذف نرم)، موبایل ۲۵۲ پاس (۲ تستِ کیف‌سنک)
- ℹ️ **دسته‌ها:** همراهِ خودِ تراکنش (allocations) به سرور و بین اعضا می‌روند؛ جدولِ مستقلِ Category روی سرور فعلاً مصرف‌کننده ندارد. **بودجه‌ها:** هنوز در اپ قابلیتِ بودجه نداریم — کارِ فاز بعد
- ⬜ **کارِ خودت:** `sudo economyctl update && sudo economyctl migrate` (مایگریشن `accounts.0003_wallet`)، سپس نصب APKِ جدید

## خواسته‌های بعدی (اختیاری)
- ✅ ساخت کاربر و عضو خانواده از پنل مدیریت (ثبت‌نام داخل اپ عمداً وجود ندارد)
- ⬜ اعلانِ «تراکنش جدید» روی گوشی صاحب کارت وقتی پیامکش روی گوشی دیگری رسیده
- 🔒 فاز ۷ (تکمیل): سرشماره‌های عددی بانک‌ها — نیازمند نمونه‌ی بیشتر
- 🔒 فاز ۱۲: تولید (VPS، HTTPS، بکاپ) — نیازمند سرور

## یادداشت‌ها
- بعد از به‌روزرسانی کد بک‌اند: `manage.py migrate` (مایگریشن 0002 روی دیتابیس توسعه اجرا شد).
- IP کامپیوتر سرور: 192.168.1.100 (در app_config.dart). اگر عوض شد، آن‌جا یا با --dart-define بده.
- APK فعلی release امضاشده با کلید debug است (برای تست شخصی). نسخه‌ی فروشگاهی بعداً با کلید release.

## بودجه‌ها + استقرار خودکار روی سرور (۱۴۰۵/۰۶/۲۴ - دور چهارم)
- ✅ **بودجه‌ها (سقفِ ماهانه‌ی هر دسته):** مدلِ سرور بر اساسِ نامِ دسته (هم‌راستا با allocations) + endpoint `/budgets/sync/` (upsert idempotent + دریافت، بین اعضای خانواده مشترک)؛ اپ: جدولِ budgets (schema v9)، صفحه‌ی «بودجه‌ها» در تنظیمات با نوارِ مصرف/سقف و هشدارِ عبور، سنکرونِ push/pull
- ✅ **تست:** بک‌اند ۷۹ پاس (۴ تستِ BudgetSync)، موبایل ۲۵۷ پاس (۴ تستِ بودجه + سنکرون)
- ✅ **نقش‌ها روی سرور اصلاح شد:** هر خانواده یک مالک؛ ownerهای اضافه (ALI, ABOL) به عضو تبدیل شدند
- ✅ **استقرار کامل روی سرور از راه SSH:** کد + مایگریشن‌ها (accounts.0003_wallet، budgets.0004) + economyctl تازه + بلوک `/updates/` در nginx؛ APKِ ۱٫۰٫۴ منتشر شد
- ⚠️ APKِ ۱٫۰٫۵ (با بودجه‌ها) باید منتشر شود

## پنج خواسته‌ی مالی + شفافیتِ نسخه (۱۴۰۵/۰۶/۲۵)
- ✅ **#۱ متنِ نسخه شفاف شد:** پیغامِ بررسیِ به‌روزرسانی حالا نسخه‌ی «نصب‌شده» را هم می‌گوید (نه فقط سرور)؛ و چکِ آپدیت مستقل و زودهنگام شد تا اگر مجوز پیامک/صندوق گیر کند، باز هم اجرا شود
- ✅ **#۲ تشخیصِ انتقال بین کارت‌ها:** `TransferFinder` جفتِ برداشت+واریزِ هم‌مبلغ بین دو کارتِ خانواده در بازه‌ی ۳۰ دقیقه را می‌یابد؛ تراشه‌ی «احتمال انتقال» + صفحه‌ی تأیید؛ با تأیید هر دو «انتقال» می‌شوند و از خالص بیرون می‌روند
- ✅ **#۳ موجودیِ واقعی:** به‌جای «خالص = درآمد − هزینه» (که موجودیِ اول دوره را صفر می‌گرفت)، از «مانده»ی پیامک‌ها موجودیِ واقعی حساب می‌شود (`realBalanceRial`)؛ کارتِ خلاصه «موجودی» را بزرگ نشان می‌دهد و «خالصِ دوره» را زیرش
- ✅ **#۴ انتخاب بانک موقع «مجاز کن»:** برای سرشماره‌های عددی که خودکار تشخیص داده نمی‌شوند، موقع مجاز کردنِ فرستنده بانکش پرسیده می‌شود (دیگر لازم نیست حذف و دستی اضافه کنی)
- ✅ **#۵ چند حساب در یک بانک:** «تعیین کارت» در جزئیاتِ تراکنش — وقتی پیامک شماره ندارد و شخص چند حساب هم‌بانک دارد، تراکنش را دستی به کارتِ مشخص می‌چسبانی و این انتساب پایدار می‌ماند (schema v10، `pinned_wallet_id`)
- ✅ **تست:** موبایل ۲۶۸ پاس (تست‌های TransferFinder، موجودیِ واقعی، انتسابِ دستیِ کارت) + analyze تمیز

## بازسازیِ منطقِ مالی — قدم ۱: عیب‌یابی (۱۴۰۵/۰۷/۰۲)
ریشه‌ی مشکل: هیچ «مرجعِ حقیقتی» نبود که خروجی با آن سنجیده شود؛ هر اصلاح فقط یک علامت را جابه‌جا می‌کرد. از این به بعد **مانده‌ی بانک مرجع است** و اول با عدد می‌بینیم کجا خراب است، بعد اصلاح می‌کنیم.
- ✅ **صفحه‌ی «عیب‌یابی موجودی و پیامک‌ها»** (تنظیمات ← نیاز به توجه، و «چرا این عدد؟» روی کارت خلاصه) — فقط خواندنی، سه زبانه:
  - ✅ **موجودی:** عددِ کارت خلاصه کارت‌به‌کارت (اول دوره + درآمد − هزینه) کنارِ «آخرِ دوره طبق مانده‌ی بانک» و **اختلاف**؛ برچسبِ «اول دوره تخمینی/صفر»، «بدون مانده»، «بدون شماره»، «خارج از جمع» (`core/diagnostics/balance_breakdown.dart`، همان `realBalanceByCard` کارت خلاصه)
  - ✅ **زنجیره‌ی مانده:** هر پیامک با «مانده‌ی قبلی ± مبلغ» سنجیده می‌شود و علت گفته می‌شود: نوعِ برعکس (اختلاف = ۲×مبلغ)، مانده عوض نشده (تکراری/غیرتراکنش)، ترتیبِ جابه‌جا، نوعِ پیشنهادی برای انتقال/بازبینی، ناجور (جاافتاده/کارمزد/مبلغ اشتباه)؛ + **هشدارِ «یک حساب دو بار شمرده می‌شود»** وقتی پیامک‌های یک حساب گاهی با کارت و گاهی با شماره حساب می‌آیند (`balance_chain.dart`)
  - ✅ **پیامک‌ها:** سرنوشتِ هر پیامکِ فرستنده‌ی مجاز (شمرده شد / انتقال / بازبینی / حذف‌شده / تکراری / ثبت‌نشده / رد: رمز پویا، یادآوری، بی‌مبلغ، نوع نامعلوم) + فرستنده‌های غیرمجازِ مبلغ‌دار + **پیش‌نمایشِ قانونِ پیشنهادی کاربر** (سرشماره + شماره حساب/کارت + مبلغ + نوع) بدون اعمال (`sms_diagnosis.dart`)
  - ✅ **کپیِ گزارش** برای فرستادن: شماره‌ی کارت/حساب/شبا/موبایل پوشانده (۴ رقم آخر)، مبلغ و مانده می‌مانند؛ فقط در کلیپ‌بورد، نه سرور/لاگ (`diagnostic_report.dart`)
- ✅ نمونه‌های واقعیِ کاربر (با اعداد ساختگی) در تست: پیامکِ ملت «حساب…برداشت…مانده» درست شمرده می‌شود؛ «شارژ اعتبار دیجی‌پی» الان **به‌اشتباه هزینه** شمرده می‌شود و قانونِ پیشنهادی ردش می‌کند (نبودِ شماره حساب)
- ✅ اسکرین‌شات‌های تازه در `screenshot_test` (۱۵–۱۷) + رفعِ مسیرِ فونتِ آیکن روی لینوکس
- ✅ **تست:** موبایل ۳۰۹ پاس (۳۸ تستِ تازه‌ی عیب‌یابی) + analyze تمیز؛ نسخه `1.0.14+15`
- ✅ **کارِ خودت:** APKِ `1.0.14+15` نصب و گزارش فرستاده شد (نتیجه در بخش بعد)
- ✅ **قدم ۲:** الگوهای واقعیِ گزارش (ملت/تجارت/دیجی‌پی/دیما، ۲۴:۰۰، هم‌دقیقه‌ها) در تست‌ها (`test/diagnostics/repair_test.dart`)
- ✅ **قدم ۳ (بخش اصلی):** نسخه‌های سرور به پیامکشان وصل می‌شوند + «یکی کن» با نگاشتِ پایدار برای پیامک‌های بعدی. ⬜ مانده: گزارشِ کارت‌ها (`cardKeyOf`) هنوز برچسبِ کیف را مقدم می‌داند
- ✅ **قدم ۴ (قانون):** «سرشماره + شماره حساب/کارت + مبلغ + نوع» اعمال شد. ⬜ مانده: نوع از کلمه‌ی کنارِ مبلغ (در پیامک‌های فعلیِ کاربر مشکلی دیده نشد)، کارت‌به‌کارت با غریبه، مانده‌ی تومانی ×۱۰
- ⬜ **قدم ۵:** کارت خلاصه: موجودیِ اول و آخر مستقیم از بانک + خطِ «مغایرت» که به زنجیره لینک می‌شود

## قدم ۲ تا ۴: قانون + تعمیرِ خودکار + «درستش کن» (۱۴۰۵/۰۷/۰۲ - دور دوم)
یافته‌های گزارشِ واقعی: پارسرِ ملت/تجارت درست بود (۳ ناجور از ۱۱۷). خرابی‌ها: (۱) نسخه‌های برگشته از سرور (نصبِ قبلی) شماره‌ی حساب نداشتند و جدا شمرده می‌شدند ← «اصلی»/«حقوق» و ۶۶م موجودیِ اضافه؛ (۲) پیامک‌های بی‌شماره (اعتبار دیما/دیجی‌پی، بازگشت پول، اطلاعیه) شمرده می‌شدند؛ (۳) یک برداشتِ ۲٫۵م‌ی واقعی دستی حذف شده بود؛ (۴) پیامک‌های هم‌دقیقه ترتیبِ تصادفی داشتند؛ (۵) «24:00» اولِ روز حساب می‌شد.
- ✅ **قانون:** پیامک فقط با شماره‌ی حساب/کارت + مبلغ + نوع ثبت می‌شود (`isCountable` در `SmsImporter`)
- ✅ **تعمیرِ خودکار** (`core/diagnostics/repair.dart`)، یک بار بعد از نصب (قبل از خواندنِ صندوق) و با دکمه‌ی «اعمال دوباره‌ی قانون‌ها»: وصل کردنِ نسخه‌های سرور به پیامکشان با اثرانگشت (متن + شماره‌ی حساب برمی‌گردد، `origin=local`)، تکمیلِ شماره/مانده/تاریخ از متن، حذفِ نرمِ ثبت‌های خلافِ قانون (تراکنشِ «تعیین کارت»‌شده یا برگردانده‌شده دست نمی‌خورد)، و واردکردنِ پیامک‌های تراکنشیِ ثبت‌نشده (تا ۲۰۰۰ پیامک)
- ✅ **ترتیبِ هم‌دقیقه‌ها** با جور شدنِ مانده (`sortForBalance`) در موجودی، زنجیره و «ناهماهنگی مانده»؛ **«24:00»** = پایانِ روز
- ✅ **دکمه‌های «درستش کن»** در عیب‌یابی: «ثبت به‌عنوانِ واریز/برداشت» (نوعِ برعکس)، «برگرداندنِ تراکنشِ حذف‌شده» (وقتی مانده نشان می‌دهد به‌اشتباه حذف شده)، «حذف (تکراری/خلافِ قانون)»، «ثبتِ دستیِ مبلغِ جاافتاده»، «یکی کن» (دو گروهِ یک حساب؛ پیامک‌های بعدی هم با نگاشت به همان حساب)، «ثبت / واردکردنِ همه‌ی ثبت‌نشده‌ها»، «این تراکنش است؛ ثبت کن»
- ✅ همه‌ی تغییرات (حذف/برگرداندن/نوع) به سرور هم می‌رسد (`applyPatches` ← outbox)
- ✅ رفعِ باگ: `setState` در صفحه‌ی عیب‌یابی Future برمی‌گرداند و بعد از بستنِ جزئیاتِ تراکنش خطا می‌داد
- ✅ **تست:** موبایل ۳۳۲ پاس (تعمیر، ترتیب، برگرداندن، یکی کردن، دکمه‌ها، روی SQLite واقعی هم) + analyze تمیز؛ نسخه `1.0.15+16`
- ⬜ **کارِ خودت:** ایجنتِ لوکال `main` را pull کند و APKِ `1.0.15+16` را منتشر کند؛ بعد از نصب یک بار برنامه را باز کن (تعمیر خودکار اجرا می‌شود) ← عیب‌یابی ← یک **ماه** را انتخاب کن (نه «همه») ← اگر اختلاف ماند، گزارش را بفرست
- ✅ کارت خلاصه: موجودیِ آخر مستقیم از بانک + برچسبِ «مغایرت» (قدم ۵؛ بخش بعد)

## همگام‌سازیِ مقاوم، موجودیِ طبقِ بانک، فرمِ تازه‌ی کارت (۱۴۰۵/۰۷/۰۲ - دور سوم)
- ✅ **«سرور در دسترس نبود» گمراه‌کننده بود:** اپ هر خطایی (قطعیِ اینترنت، خطای ۴۰۰/۴۰۴/۵۰۰ سرور، حتی باگِ خودِ برنامه) را «در دسترس نبود» می‌گفت. حالا دقیق: «به سرور وصل نشد» / «در مرحله‌ی X سرور خطای N داد: …» / «خطای برنامه در مرحله‌ی X» (`SyncFailure`)؛ پینگ فقط زنده بودنِ ماشین را نشان می‌دهد، نه سرویسِ وب
- ✅ **یک آیتمِ خراب دیگر همه‌چیز را قفل نمی‌کند:** (۱) دسته‌ی تراکنشی که سرور رد کند تک‌تک فرستاده می‌شود؛ (۲) کیف/بودجه: نتیجه‌ی هر آیتم جدا، و در سرورِ قدیمی تک‌تک؛ کیفی که شناسه‌اش روی سرور مالِ خانواده‌ی دیگری است (بعد از عوض کردنِ حساب ← ۴۰۴) شناسه‌ی تازه می‌گیرد؛ آیتمِ نامعتبر «ناموفق» می‌شود؛ (۳) هر مرحله (ارسال/دریافت/کارت‌ها/بودجه‌ها) جدا؛ مهلتِ درخواست‌های همگام‌سازی ۶۰ ثانیه
- ✅ **سرور:** `apps/common/sync.py` (`upsert_each`): آپلودِ کیف/بودجه نتیجه‌ی هر آیتم (`created/updated/conflict/error`)؛ تراکنش‌ها هر آیتم در savepointِ خودش (خطا فقط همان آیتم، با لاگ). بک‌اند ۸۳ تست پاس
- ✅ رفعِ باگ: `first ??= await …` بعد از اولین خطای آیتم، بقیه‌ی کیف‌ها را اصلاً ثبت نمی‌کرد (تست گرفت)
- ✅ **کارت خلاصه:** عددِ اصلی = جمعِ آخرین مانده‌ی هر حساب طبقِ بانک (برای هر تعداد حساب؛ درست حتی با پیامکِ جاافتاده)، و برچسبِ «مغایرت با تراکنش‌ها: X — ببین کجاست» که عیب‌یابی را باز می‌کند
- ✅ **فرمِ کارت:** حساب‌هایی که در پیامک‌ها پیدا شده با یک لمس پر می‌شوند؛ برچسب اختیاری («ملت ۵۵۹۶»)؛ **سرشماره‌ی پیامک** همین‌جا (با بانک و صاحب مجاز می‌شود)؛ **موجودیِ دستی** برای حسابِ بی‌پیامک (حسابِ قدیمیِ سپه) که در «موجودی» جمع می‌شود
- ✅ پیامکِ «رمز: … اعتبار …» (رمزِ انتقالِ سپه) تراکنش نیست و ثبت نمی‌شود (تست) — پیش از انجامِ انتقال می‌آید، ممکن است لغو شود، و انتقال از کارتِ ملت در پیامکِ حسابِ ملت هم هست
- ✅ **تست:** موبایل ۳۴۳ پاس + analyze تمیز؛ بک‌اند ۸۳؛ نسخه `1.0.16+17`
- ⬜ **کارِ خودت:** (۱) ایجنتِ لوکال `main` را pull و APKِ `1.0.16+17` را منتشر کند؛ (۲) روی سرور `sudo economyctl update` (بدون مایگریشن) تا تغییراتِ سرور هم اعمال شود؛ (۳) اگر همگام‌سازی باز خطا داد، **متنِ دقیقِ پیام** را بفرست (حالا مرحله و کد را می‌گوید) و `sudo economyctl logs` همان لحظه

## پیامک‌های قدیمی‌ترِ بانک ثبت نمی‌شدند (۱۴۰۵/۰۷/۰۲ - دور چهارم، تستِ کاربرِ دوم)
- ✅ **علت:** بعد از «مجاز کن» (و افزودن/برداشتنِ دوباره‌ی فرستنده) فقط **۳۰۰ پیامکِ آخرِ کلِ صندوق** خوانده می‌شد؛ پیامک‌های قدیمی‌ترِ همان بانک هیچ‌وقت ثبت نمی‌شد (کاربرِ دوم: از چهار بانک فقط دو پیامکِ ملت). پیشنهادِ فرستنده‌ها هم فقط از همان ۳۰۰ تا بود
- ✅ **حالا:** مجاز کردن/افزودنِ فرستنده (از فرمِ کارت هم) ← **کلِ صندوق**؛ بازشدنِ اپ ← فقط پیامک‌های بعد از آخرین خواندن (سریع، `inbox_watermark`)؛ اولین بازشدن بعد از این نسخه یک بار کلِ صندوق را می‌خواند؛ پیشنهادِ فرستنده و عیب‌یابی از ۵۰۰۰ پیامکِ آخر
- ✅ صفحه‌ی «فرستنده‌های پیامک بانک»: دکمه‌ی «خواندنِ دوباره‌ی همه‌ی پیامک‌ها»، تعدادِ تراکنشِ هر فرستنده، «چرا بعضی پیامک‌ها ثبت نشده؟» (← عیب‌یابی/پیامک‌ها)، و برداشتنِ فرستنده «فقط بردار» یا «بردار و تراکنش‌هایش را حذف کن»
- ✅ **تست:** موبایل ۳۵۰ پاس (سناریوی ۴۰۰ پیامک که پیامک‌های بانک پشتِ آن‌هاست) + analyze تمیز؛ نسخه `1.0.18+19`
- ⬜ **کارِ خودت:** بعد از نصب روی گوشیِ کاربرِ دوم: «فرستنده‌های پیامک بانک» ← تعدادِ تراکنشِ هر بانک را ببین؛ اگر بانکی هنوز صفر است ← «چرا بعضی پیامک‌ها ثبت نشده؟» و دلیلش را (یا گزارشِ کپی‌شده را) بفرست

## قالبِ پیامکِ پاسارگاد و سپه + نویسه‌های نامرئی (۱۴۰۵/۰۷/۰۲ - دور پنجم، نمونه‌های کاربرِ دوم)
- ✅ با نمونه‌های واقعی: **پاسارگاد** اصلاً خوانده نمی‌شد (مبلغ با علامتِ آخر «7,400,000-»، حسابِ نقطه‌دار)؛ **سپه** شماره‌ی حساب نداشت از نظرِ پارسر (حساب در خطِ بعد از «به:») و با قانون رد می‌شد؛ هر دو حالا خوانده می‌شوند (fixtureهای `real_format_pasargad_*`، `real_format_sepah_interest`)
- ✅ **نویسه‌های نامرئیِ جهت‌دهی** (RLM و …) قبل از پارس و تطبیقِ فرستنده حذف می‌شوند — محتمل‌ترین علتِ ثبت‌نشدنِ دو پیامکِ سودِ ملتِ ۱ مهر (پارس‌شان بدون این نویسه‌ها درست است)؛ اثرانگشتِ پیامک عمداً دست‌نخورده
- ✅ `kParserVersion = 2`: اولین بازشدن بعد از این نسخه کلِ صندوق را دوباره می‌خواند تا پیامک‌های قبلاً ردشده ثبت شوند
- ✅ پوشاندنِ حسابِ نقطه‌دار در گزارش؛ برچسبِ پیش‌فرض از رقم‌ها
- ✅ **تست:** موبایل ۳۵۹ پاس + analyze تمیز؛ نسخه `1.0.19+20`
- ⬜ **کارِ خودت:** نصب روی گوشیِ کاربرِ دوم و یک بار باز کردن ← «فرستنده‌های پیامک بانک» تعدادِ تراکنشِ هر بانک؛ اگر پیامکِ ملتِ ۱ مهر هنوز نیامد ← «چرا بعضی پیامک‌ها ثبت نشده؟» و وضعیتِ همان دو پیامک را بفرست

## ملتِ کاربرِ دوم + خطای «دانلود ناموفق» (۱۴۰۵/۰۷/۰۲ - دور ششم)
- ✅ **پیامکی که یک بار حذف شده بود دیگر هیچ‌وقت ثبت نمی‌شد:** ضدتکرار حذف‌شده‌ها را هم می‌بیند (تا حذفِ دستیِ کاربر برنگردد)، پس پیامکی که **خودِ برنامه** کنار گذاشته بود (تعمیرِ خودکار با پارسرِ قدیمی، «بردار و تراکنش‌هایش را حذف کن») نه با «خواندنِ دوباره» برمی‌گشت نه با مجاز کردنِ دوباره‌ی فرستنده. حالا شناسه‌ی حذف‌های خودکار نگه داشته می‌شود (`auto_removed`) و هر وقت با قانون بخوانند خودکار برمی‌گردند؛ حذفِ دستیِ کاربر هرگز. تعمیرِ خودکار با هر پارسرِ تازه یک بار دوباره اجرا می‌شود
- ✅ **پارسر (`kParserVersion = 3`):** با تستِ «هر نویسه در هر جای پیامک» پیدا شد که Word Joiner، نیم‌خطِ نرم، کشیده «ـ»، اعراب، variation selector و … بین کلمه و عدد پارس را خراب می‌کرد، و «86،184» / «1.500.000» مبلغ را ۸۶ / ۱ می‌خواند؛ هر دو درست شد (فقط برای پارس؛ اثرانگشت دست‌نخورده)
- ✅ گزارشِ عیب‌یابی نویسه‌های نامرئیِ هر پیامک را نام می‌برد (اگر باز پیامکی ثبت نشد، علت در گزارش پیداست)
- ✅ حذف‌هایی که نسخه‌های ۱٫۰٫۱۸/۱۹ با «بردار و تراکنش‌هایش را حذف کن» کرده بودند سابقه ندارند (خودکار برنمی‌گردند چون از حذفِ دستیِ تکراری‌ها قابلِ تشخیص نیستند)؛ برایشان در عیب‌یابی ← «پیامک‌ها» دکمه‌ی «برگرداندنِ N پیامکِ حذف‌شده که با قانون می‌خوانند» آمد
- ✅ **«دانلود ناموفق» در به‌روزرسانی:** لینکِ APK (`http://koalaverifyshop.ir/…`) روی HTTP است و دانلودگرِ بومیِ اندروید (flutter_downloader) HTTP بی‌رمز را برای دامنه‌ای که در `network_security_config` نیست رد می‌کند (Dio این محدودیت را ندارد؛ برای همین قبلاً کار می‌کرد)؛ نوشتن در «دانلودها» هم در اندروید ۱۰+ مجوز می‌خواهد. برگشت به دانلود با Dio در پوشه‌ی خودِ برنامه با نوارِ پیشرفت و «لغو»، دامنه به فهرستِ HTTP مجاز اضافه شد، اگر دامنه در دسترس نبود همان فایل از سرورِ version.json، بررسیِ اینکه فایل واقعاً APK است (نه صفحه‌ی خطا/فیلتر)، و در خطا پیامِ فارسیِ دقیق + «دوباره» + لینک برای مرورگر. flutter_downloader حذف شد
- ✅ شماره‌حساب‌های واقعیِ اسکرین‌شات‌ها از تست‌ها/مستندات برداشته و با شماره‌ی ساختگی عوض شد
- ✅ **تست:** موبایل ۴۳۷ پاس + analyze تمیز؛ نسخه `1.0.20+21`
- ⬜ **کارِ خودت:** (۱) ایجنتِ لوکال `main` را pull و APKِ `1.0.20+21` را منتشر کند؛ (۲) روی گوشی‌هایی که ۱٫۰٫۱۷ تا ۱٫۰٫۱۹ دارند دانلودِ درون‌برنامه همان خطا را می‌دهد (کدِ قدیمی)، پس این یک بار APK را با مرورگر از `http://koalaverifyshop.ir/updates/economy-latest.apk` بگیر و نصب کن؛ از این نسخه به بعد درون‌برنامه کار می‌کند؛ (۳) بعد از نصب روی گوشیِ کاربرِ دوم اگر پیامکِ ملتِ ۱ مهر هنوز نیامد: عیب‌یابی ← «کپیِ گزارش» و بفرست (حالا نویسه‌های نامرئی را هم نشان می‌دهد)

## گزارشِ عیب‌یابیِ کاربرِ دوم: ملت، پاسارگادِ دیجیتال، اختلافِ ۹۲ میلیون (۱۴۰۵/۰۷/۰۲ - دور هفتم)
- ✅ **ملت «بدون شماره» و دو پیامکِ ۱ مهرِ «حذف‌شده»:** برنامه نصب یا ورود دوباره شده بود؛ تراکنش‌ها از سرور برگشتند بدونِ متن و شماره‌ی حساب (عمداً به سرور نمی‌روند). خواندنِ صندوق فقط متن را به آن‌ها وصل می‌کرد، نه شماره را، و تعمیر ردیف‌های «سرور» را کلاً رد می‌کرد. دو پیامکِ ۱ مهر روی سرور حذف‌شده بودند (از نصبِ قبلی). حالا: شماره/مانده/بانک از همان پیامک پر و ردیف «محلی» می‌شود (هم هنگامِ خواندن، هم در تعمیر)
- ✅ **«برگرداندنِ همه» تکراری‌ها را برنمی‌گرداند** (پیامکی که بانک دوباره فرستاده: همان متن، یا همان حساب/مبلغ/مانده) — در ۱٫۰٫۲۰ دو پیامکِ تکراریِ ۵٬۱۰۰٬۰۰۰ پاسارگاد را هم برمی‌گرداند
- ✅ **اثبات با مانده‌ی بانک:** پیامک‌های «بانکداری دیجیتال پاسارگاد» (واریزِ وام از حساب پشتوانه، پرداختِ قسط، کارمزد) شماره‌ی حساب ندارند و رد/حذف می‌شدند، در حالی که مانده‌شان تا ریال با زنجیره‌ی همان حساب جور است — علتِ بیشترِ اختلافِ ۹۲ میلیونی و ۱۱ ناجوریِ زنجیره. حالا شمرده می‌شوند، فقط وقتی مانده دقیقاً یک حساب را ثابت کند (بی‌بانک مثلِ دیجی‌پی هرگز)؛ حذف‌شده‌های واقعی (۱٬۸۵۰٬۰۰۰ پاسارگاد، سودِ ۴۲٬۴۲۳ سپه) هم با همین شاهد برمی‌گردند؛ حذفِ دستیِ کاربر از این نسخه به بعد هرگز
- ✅ پارسر (`kParserVersion = 4`): «موجودی حساب دیجیتال: X» مانده است و «تعداد اقساط باقی مانده:0» نه؛ زمانِ پاسارگاد «05/27_12:50» خوانده می‌شود (پیامک‌های دیر رسیده دیگر جابه‌جا در زنجیره نمی‌افتند)؛ متنِ فقط‌تاریخ زمانِ رسیدنِ همان روز را می‌گیرد؛ fixtureهای پاسارگاد به ترتیبِ واقعیِ متن درست شد
- ✅ عیب‌یابی: «قبول (حساب از مانده‌ی بانک)»، و شمارِ «ثابت با مانده» در گزارش و کارتِ تعمیر
- ✅ **تست:** موبایل ۴۵۳ پاس + analyze تمیز (سناریوی واقعیِ ۲۲ مرداد تا ۸ شهریور با شماره‌ی ساختگی)؛ نسخه `1.0.21+22`
- ⬜ **کارِ خودت:** (۱) ایجنتِ لوکال APKِ `1.0.21+22` را منتشر کند؛ (۲) روی گوشیِ کاربرِ دوم به‌روزرسانی (از ۱٫۰٫۲۰ درون‌برنامه کار می‌کند) و یک بار باز کردن — تعمیر خودکار اجرا می‌شود؛ (۳) عیب‌یابی ← «پیامک‌ها» ← «برگرداندنِ ۲ پیامکِ حذف‌شده…» (دو سودِ ملتِ ۱ مهر)؛ (۴) گزارشِ تازه را بفرست تا ناجوری‌های باقی‌مانده را ببینم

## «از کجا بفهمم برنامه روی گوشیِ بقیه درست کار می‌کند؟» — سلامتِ گوشی‌ها (۱۴۰۵/۰۷/۰۳ - دور هشتم)
- ✅ **چرا ملت فقط روی گوشیِ زهرا خراب بود:** کد یکی است؛ داده‌ی دو گوشی فرق دارد. روی گوشیِ زهرا تراکنش‌ها بعد از نصب/ورودِ دوباره از سرور برگشته بودند (بی‌شماره) و دو پیامکِ ۱ مهر روی نصبِ قبلی حذف شده بود؛ روی گوشیِ مهدی همه از اول از پیامک ثبت شده‌اند (درست‌کردنش در ۱٫۰٫۲۱)
- ✅ **سلامتِ هر گوشی** (`core/diagnostics/device_health.dart`): حکمِ سالم / نیاز به بررسی / مشکل با دلیل و «کجا درستش کنم»: بی‌مجوزِ پیامک، بانکی انتخاب نشده، پیامکِ تراکنشیِ ثبت‌نشده، بانکی که هیچ پیامکش شمرده نمی‌شود، پیامکِ درستِ حذف‌شده، تراکنشِ بی‌شماره، ناجوریِ زنجیره‌ی مانده، همگام‌سازیِ ناموفق/قدیمی؛ به‌علاوه‌ی بانک‌ها (پیامک/شمرده) و حساب‌ها (مانده‌ی بانک)
- ✅ هر گوشی هر ۶ ساعت (و با بازکردنِ عیب‌یابی) خلاصه را به سرور می‌فرستد — **بدونِ متنِ پیامک، شماره‌ی حساب/کارت و سرشماره** (تست)
- ✅ سرور: مدلِ `DeviceHealth` و `GET/POST /api/v1/family/health/` (مدیر همه‌ی اعضا، عضو فقط خودش؛ اعتبارسنجی و سقفِ اندازه؛ مایگریشنِ `families/0004`)؛ بک‌اند ۸۸ تست پاس
- ✅ اپ: تنظیمات / «سلامتِ برنامه روی گوشی‌ها» (این گوشی الان + گوشی‌های بقیه، گزارشِ قدیمی علامت می‌خورد) و کارتِ «وضعیتِ کلیِ این گوشی» بالای عیب‌یابی
- ✅ فلشِ «←» که با فونت نمایش داده نمی‌شد در متن‌های صفحه با «/» عوض شد
- ✅ **تست:** موبایل ۴۶۳ پاس + analyze تمیز + ۱۸ اسکرین‌شات؛ نسخه `1.0.22+23`
- ⬜ **کارِ خودت:** (۱) روی سرور `sudo economyctl update` (این بار **مایگریشن دارد**؛ همین دستور اجرایش می‌کند)؛ (۲) ایجنتِ لوکال APKِ `1.0.22+23` را منتشر کند؛ (۳) روی هر گوشی به‌روزرسانی و یک بار باز کردن (با اینترنت)؛ (۴) روی گوشیِ خودت: تنظیمات / «سلامتِ برنامه روی گوشی‌ها»

## نسخه‌ی ۲: دفترِ حساب با تأییدِ کاربر (طرحِ مصوب ۱۴۰۵/۰۷/۰۳ — `docs/v2-design.md`)
تصمیم‌های کاربر: تاریخِ شروع = اولِ ماه؛ همه‌ی پیامک‌ها منتظرِ تأیید؛ نصبِ دوباره/به‌روزرسانی هیچ چیز را خراب نکند؛
موجودیِ الانِ هر حساب مبنا و اختلاف‌ها (حتی ورودِ دستیِ اشتباه) دیده شوند. نسخه‌ی ۱ منجمد (فقط باگِ بحرانی).
- ⬜ **فاز ۱ — هسته‌ی خالص:** `lib/core/ledger/` (مدل‌ها، `ledger_math`، `suggestion`، `sms_intake`، مخزن و مایگریشن، پشتِ پرچمِ `ledger_v2`)؛ تستِ سناریوهای هسته (بخشِ ۱۲)
- ⬜ **فاز ۲ — تأیید:** صفحه‌ی «منتظرِ تأیید» (ثبت/ویرایش/تراکنش نیست/تکراری، کشیدن، تأییدِ گروهی با دست)، افزودنِ دستی، ساختِ حساب با «موجودیِ الان»، نوتیفیکیشنِ دکمه‌دار؛ تست روی گوشی
- ⬜ **فاز ۳ — اختلاف:** جزئیاتِ حساب، پنجره‌های اختلاف (نارنجی/قرمز) و دکمه‌هایش، «تطبیق با موجودیِ واقعی»، گزارشِ ماه؛ سناریوهای ۳ تا ۶ روی گوشی
- ⬜ **فاز ۴ — سرور:** مدل‌های نسخه‌ی ۲ (حساب، نقطه، تراکنش، تصمیمِ پیامک، تاریخِ شروع) + همگام‌سازی؛ سناریوهای ۷ تا ۹ با حذف و نصبِ دوباره روی دو گوشی
- ⬜ **فاز ۵ — پایان:** خانواده و سلامت روی نسخه‌ی ۲، برداشتنِ پرچم، حذفِ کدِ نسخه‌ی ۱ (فهرستِ بخشِ ۱۰)

