# قوانین پروژه‌ی economy

این فایل هر سشن خوانده می‌شود. این قوانین را بدون یادآوری مجدد رعایت کن.

## کامیت و پوش
- **همه‌ی تغییرات را کامیت کن، بدون هیچ اثری از دستیار.** پیام کامیت هیچ trailer یا امضایی مثل `Co-Authored-By` یا «Generated with Claude Code» نداشته باشد. کامیت‌ها فقط به نام خود کاربر (Mahdi).
- پیام کامیت کوتاه و فارسی/انگلیسی فنی، توصیف واقعی تغییر.
- هنوز remote اضافه نشده. **به‌محض اینکه کاربر ریپوی remote را اضافه کرد، پوش کن.**
- روی شاخه‌ی فعلی (`master`) کار کن مگر کاربر خلافش را بگوید.

## کیفیت و پیشرفت فاز
- **قبل از رفتن به فاز بعد، باید عملاً تست کنی که کار فعلی بدون باگ است.** با ایراد یا باگ به فاز بعد نرو.
- «تست عملی» یعنی اجرای واقعی: تست‌های واحد پاس شوند، و هرجا ممکن است روی دستگاه/امولاتور اجرا شود. صرفِ نوشتن کد کافی نیست.
- بعد از هر پیشرفت، `TODO.md` را به‌روزرسانی کن (زده‌شده‌ها ✅، مانده‌ها ⬜) تا وضعیت روشن بماند.

## زبان
- پاسخ‌ها به کاربر فارسی باشند. کد و شناسه‌ها انگلیسی.

## معماری (مرجع کامل در docs/)
- بک‌اند: Django REST + PostgreSQL. موبایل: Flutter + SQLite. معماری Offline-First.
- سرور فعلی = کامپیوتر خود کاربر (`docs/dev-setup.md`)؛ بعداً VPS ایرانی.

## پارسر پیامک (اولویت کاربر)
- ترتیب قطعی: **اول تشخیص بانک از روی سرشماره/فرستنده، بعد استخراج مبلغ و بقیه‌ی فیلدها از متن.**
- پارسر per-bank باشد (نه یک Regex غول‌آسا). جزئیات در `docs/sms-parsing.md`.
- متن خام پیامک هرگز لاگ یا به سرور فرستاده نشود.
